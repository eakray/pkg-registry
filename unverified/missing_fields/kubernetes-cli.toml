name = "kubernetes-cli"
schema_version = "1"
version = "kubernetes.git,"

[binaries.darwin.x64.binary]
strip_prefix = 1
url = "https://github.com/kubernetes/kubernetes.git,"

[meta]
description = "Kubernetes command-line interface"
homepage = "https://kubernetes.io/docs/reference/kubectl/"
license = "Apache-2.0"
platforms = ["darwin"]
