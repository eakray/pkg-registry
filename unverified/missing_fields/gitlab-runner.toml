name = "gitlab-runner"
schema_version = "1"
version = "gitlab-runner.git,"

[binaries.darwin.x64.binary]
strip_prefix = 1
url = "https://gitlab.com/gitlab-org/gitlab-runner.git,"

[meta]
description = "Official GitLab CI runner"
homepage = "https://gitlab.com/gitlab-org/gitlab-runner"
license = "MIT"
platforms = ["darwin"]
